apiVersion: v1
kind: Service
metadata:
  name: nginx-deployment
  namespace: three-tier
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
